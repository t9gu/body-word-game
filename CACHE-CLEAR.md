# 🔄 清除浏览器缓存指南

## ⚠️ 重要提示

如果您看到以下错误，说明浏览器正在使用旧的缓存文件：
- `position.clone is not a function`
- `favicon.ico 404错误`
- MediaPipe初始化失败

## 🚀 快速解决方案

### 方法1：访问清除缓存页面（最简单）
```
http://localhost:8000/clear-cache.html
```
点击按钮自动清除缓存并重新加载游戏。

### 方法2：硬刷新浏览器（推荐）

**Mac用户：**
```
Cmd + Shift + R
或
Cmd + Option + R
```

**Windows/Linux用户：**
```
Ctrl + Shift + R
或
Ctrl + F5
```

### 方法3：开发者工具（最彻底）

1. 按 `F12` 打开开发者工具
2. 右键点击浏览器刷新按钮
3. 选择 **"清空缓存并硬性重新加载"**

### 方法4：禁用缓存（开发时使用）

1. 按 `F12` 打开开发者工具
2. 进入 **Network（网络）** 标签
3. 勾选 **"Disable cache（禁用缓存）"**
4. **保持开发者工具打开**，刷新页面

## ✅ 验证修复是否生效

刷新后，在浏览器控制台（F12）中应该看到：

```
✅ ThreeSceneManager v2.0 已加载
✅ PoseDetector v2.0 已加载
```

如果看到这些消息，说明新文件已正确加载！

## 🔍 检查是否仍有问题

如果清除缓存后仍有错误：

1. **检查控制台日志**：确认看到 v2.0 版本标记
2. **检查Network标签**：确认JS文件URL包含 `?v=2` 参数
3. **完全关闭浏览器**：关闭所有标签页，重新打开
4. **尝试隐私/无痕模式**：这会强制使用新文件

## 📝 文件版本信息

当前版本：**v2.0**

修复内容：
- ✅ 修复了 `position.clone` 错误（Three.js）
- ✅ 改进了 MediaPipe Pose 初始化流程
- ✅ 添加了 favicon.svg 文件
- ✅ 添加了版本标记和缓存破坏机制

## 🛠️ 开发者注意事项

每次修改JavaScript文件后：
1. 更新 `index.html` 中的版本号（`?v=X`）
2. 更新文件开头的版本注释
3. 提醒用户清除缓存

---

**如果问题仍然存在，请联系开发者并提供控制台完整错误信息。**
